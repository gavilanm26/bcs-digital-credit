import { test } from '@playwright/test'
//import {landing} from ".user-interfaces/homePage";

test.describe('must show login to validate digital channels', () => {
   // let landing: landing

    test.beforeEach(({page:page}) =>[
     //   landing = new landing()

    ])
})